{
  "name": "brew-cms-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Open-source, agent-ready content operating system",
  "license": "Apache-2.0",
  "author": "Victor Kuldeep and BrewCMS Contributors",
  "scripts": {
    "build": "pnpm -r run build",
    "typecheck": "tsc --build --verbose",
    "lint": "eslint . --max-warnings 0",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "pnpm -r run clean"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "prettier": "^3.4.2",
    "rimraf": "^6.0.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  }
}
